---
import { Icon } from "astro-icon/components";
import Antigravity from "./react/Antigravity/Antigravity.jsx";
import siteConfig from "../siteConfig.json";

interface SocialLink {
  platform: string;
  url: string;
  icon: string;
}

interface Props {
  section_label: string;
  heading: string;
  subheading: string;
  email_label: string;
}

const { section_label, heading, subheading, email_label } = Astro.props;

const { email, socials } = siteConfig as {
  email: string;
  socials: SocialLink[];
};

const targetSocials = socials.filter((s) =>
  ["github", "instagram"].includes(s.icon),
);

const instagram = targetSocials.find((s) => s.icon === "instagram");
const github = targetSocials.find((s) => s.icon === "github");

const emailParts = email.split("@");
const emailUser = emailParts[0];
const emailDomain = emailParts[1] || "";
---

<section
  id="contact"
  class="section contact-section"
  aria-labelledby="contact-heading"
>
  <div class="antigravity-background">
    <Antigravity
      client:visible
      count={250}
      magnetRadius={6}
      ringRadius={7}
      waveSpeed={0.4}
      waveAmplitude={1}
      particleSize={1}
      lerpSpeed={0.05}
      color="#00E5FF"
      autoAnimate={true}
      particleVariance={1}
    />
  </div>
  <div class="container">
    <div class="contact-inner">
      <p class="section-label reveal">
        {section_label}
      </p>

      <h2 id="contact-heading" class="contact-heading reveal reveal-delay-1">
        {heading}
      </h2>

      <p class="contact-subheading reveal reveal-delay-2">
        {subheading}
      </p>

      <div class="contact-socials reveal reveal-delay-3">
        {
          instagram && (
            <a
              href={instagram.url}
              target="_blank"
              rel="noopener noreferrer"
              class="contact-social-btn"
              aria-label="Instagram"
              title="Instagram"
            >
              <Icon
                name="bx:bxl-instagram"
                class="contact-icon"
                aria-hidden="true"
              />
              <span class="sr-only">Instagram</span>
            </a>
          )
        }

        <button
          class="contact-social-btn email-btn"
          aria-label={email_label}
          title={email_label}
          onclick={`window.location.href='mailto:${emailUser}' + '@' + '${emailDomain}'`}
        >
          <Icon
            name="bx:bxs-envelope"
            class="contact-icon"
            aria-hidden="true"
          />
        </button>

        {
          github && (
            <a
              href={github.url}
              target="_blank"
              rel="noopener noreferrer"
              class="contact-social-btn"
              aria-label="GitHub"
              title="GitHub"
            >
              <Icon
                name="bx:bxl-github"
                class="contact-icon"
                aria-hidden="true"
              />
              <span class="sr-only">GitHub</span>
            </a>
          )
        }
      </div>
    </div>
  </div>
</section>

<style>
  @import "../styles/components/ContactSection.css";
</style>
